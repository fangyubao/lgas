# Makefile 固件全局勘察报告

日期：2026-08-17

对照工程：`E:\code\stm32\las_new`

当前工程：`E:\code\stm32\lag_gas_mkf`

## 结论

Makefile 与 Keil 工程使用的业务源码集合、芯片宏和 HAL 配置基本一致。Keil 的 AXF/MAP 证明其 RTC 实际也使用 DS1302 软件时序后端，而不是 STM32 内部 RTC。Makefile 迁移后暴露出的主要问题集中在编译器相关位域布局、被优化的空延时、DGUS 请求响应串扰、错误的 DGUS 0x83/0x82 命令、数组发送长度以及 Flash 非对齐访问。

本轮已修复所有能够由源码、Keil MAP、DGUSII 协议和严格编译明确证实的问题。最终固件已完成普通构建和加强告警全量构建。硬件相关部分仍需按报告末尾的实机清单回归，不能仅凭静态检查宣称完全无缺陷。

## 已修复的高优先级问题

1. 标定页状态命令错误

   原来的 `{0x83, 0x29, 0x00, 0x81/0x82}` 是“读 VP”命令，不是写状态命令，而且 0x81/0x82 超出了 DGUS 0x83 指令允许的读取字数范围。它不会清除 `0x2900` 的旧状态，因此重新进入页面仍显示上次成功，表现为点击后立即成功。

   现在每次开始标定写入 `0x0080`（读取中），稳定成功后写入 `0x0081`，通信失败或超时写入 `0x0082`。三种状态均使用正确的 0x82 写 VP 指令。

2. “读取中/成功/失败”截字、残留和 GBK 乱码

   原数组长度与发送宏不一致：读取中和未稳态文本被截断，成功和失败文本存在越界读取且没有完整清空文本框。现统一为 17 字节，成功/失败数组补零到完整字段长度。

3. DGUS 请求被触摸帧或其他状态机抢占

   原 `dgusii_request()` 把收到的下一帧当作请求应答，触摸上传、页面上传或另一项 VP 读取都可能错误完成当前请求；超时还会清掉无关的待发送 UI 数据。现在按命令和 VP 匹配应答，使用独立应答缓存，并只重置本次请求。

4. RTC 每秒打印 `HMI RTC fail`

   Keil 的 AXF/MAP 确认实际使用 DS1302。审计中曾将原驱动改成 DWT 定时、多次重试和更严格的读数校验，但实机持续报错证明该改动未经硬件验证。现已撤回：Makefile 工程的 `DEVICE/rtc/rtc.c` 与 Keil 工程逐行一致，保留 Keil 已验证的 DS1302 时序和校验逻辑。

5. C2H2 再次进入页面沿用上次稳定窗口

   C2H2 的 10 点窗口、filled、斜率状态原为静态变量，下一次测量可能直接沿用上次数据而立即判定成功。现为每次新测量分配序号并清空全部窗口状态。

6. 测量成功后立刻变读取失败

   浓度成功显示后，代码还会进入记录上传；当 RTC 或后续上传失败时，旧逻辑又把 HMI 覆盖成读取失败。现测量成功和记录上传结果解耦，后续记录失败不再覆盖已经确认的测量成功。

7. 485 完成结果被其他任务丢弃

   主循环中 USB 在 dataup 之前执行。原状态机在 DONE/FAIL 后遇到任意不同请求会直接清除结果，真正发起者可能永远收不到完成状态。现为原请求保留结果 500 ms；原请求领取后立即释放，超时才允许其他请求接管。

8. 仅保留 C2H2 但预热基线仍读取其他传感器

   `start_task_run()` 虽已屏蔽其他器件，`start_task_readbase()` 仍依次读取 CH4、H2 和 C2H6。现预热基线也只读取 C2H2，避免缺失器件阻止系统进入 RUNING。

9. Flash 未对齐和越界读取

   多处把任意字节缓冲区强制转换为 `uint32_t *`，15 字节日志会被读成 16 字节，存在对象末尾越界、未对齐访问和严格别名 UB。现按最多 4 字节复制到填充为 `0xFFFFFFFF` 的局部 word 后烧写；参数读取按真实长度复制；日志扫描会拒绝损坏的超长记录。

10. 链接镜像边界和 RAM 段布局

    `_flash_image_end` 现在包括 `.data` 的 Flash 初值镜像，USB 修改存储地址时不会把参数区放到固件映像中。`.bss` 和 heap/stack 使用独立 RAM load segment，最终链接不再出现 LMA 调整或段无法分配警告。

11. USB 命令和输入边界

    `TASK_WRITE_PARAM` 最小帧长从 10 修为 11；命令解析先验证至少 2 字节；设置时间检查每个 BCD 半字节；C2H6 数据长度受接收缓冲区和本地数组双重限制；格式串类型和 CDC 分包有符号性已修正。

12. 新增清除四组参数命令

    USB 帧 `78 1B` 对应 `TASK_CLEAR_PARAM`，将 Flash 中四组 `(k,b)` 全部写为 `0.0f`，成功回复 `param cleared\r\n`。

13. 其他 GCC/Keil 差异

    HMI 轮询标志、USB 轮询标志、C2H6 等待标志和 dataup 等运行状态不再依赖实现定义的 C 位域布局；未初始化的 `fail_n` 已初始化；启动错误 ID 正确输出十六进制 A-F；记录上传长度补上了前置的 2 字节协议头。

## 构建与产物核验

- 普通构建：`make -j8`，成功。
- 严格构建：`-Wextra -Wformat=2 -Wshadow -Wundef -Wpointer-arith -Wcast-align -Wstrict-prototypes -Wmissing-prototypes`，成功。
- 固件占用：text 65336 B，data 1124 B，bss 33688 B。
- Flash 镜像结束：`0x080103A4`。
- 参数/日志默认区域：`0x08036000`，与固件镜像保持大幅安全间隔。
- BIN：`build\lgas.bin`，66468 B。
- BIN SHA-256：`0677FF114D758111B62BD89FAD2C283636851750FF84DB57AE4B431F395C8901`。
- 链接后 RAM：`.bss` 结束于 `0x200081F8`，预留 heap/stack 后仍低于 `0x20010000`。

严格构建剩余告警为未启用功能的静态函数、Cube/HAL 的未使用参数、系统调用缺少外部原型和 HAL 内部 16 位指针对齐转换。当前业务代码未再出现格式串类型错误、数组长度警告、未初始化变量警告或链接段分配错误。

## 实机回归清单

建议按下列顺序执行，每项保留 USB 日志和 HMI 录像：

1. 冷启动，确认泵清洗、C2H2 启动检测、预热结束和进入主页面。
2. 两个带时间的页面各停留 10 秒，确认每秒更新且不再出现 `HMI RTC fail`。
3. 连续进入/退出 C2H2 分析页 5 次，确认每次先“等待稳态读取”，不会沿用上次成功。
4. 标定页连续测试 5 次：点击后应为读取中；达到稳定后才成功；断开 C2H2 时应失败；重新接入后应重新从读取中开始。
5. 成功显示后继续观察 10 秒，确认不会被后续记录上传覆盖为失败。
6. 测量期间发送 USB C2H2 查询，确认 HMI 与 USB 两个请求均能最终完成，没有永久 BUSY。
7. 使用 `78 1B` 清参数，再用 `78 1A` 读取，确认四组 k、b 全为 0。
8. 重启后再次读取四组参数，确认 Flash 持久化正确。
9. 写入一组参数、生成多条日志、读取日志并清除日志，确认参数区和日志区互不覆盖。

## 尚需注意

- 请先烧录本次恢复 Keil DS1302 实现后的新固件。如果新固件仍持续打印 `HMI RTC fail`，下一步应打印 DS1302 六个原始寄存器值，再根据读回值区分时序、CH 停振位、IO 悬空或日期数据非法。USART3 和 RTC 是不同硬件链路，不能互相证明正常。
- 工程内没有可运行的单元测试或硬件在环脚本，因此传感器稳定阈值、真实 485 电气时序和串口屏控件绑定必须依赖上述实机回归。
- 当前仅启用 C2H2，CH4/H2/C2H6 源码仍保留以便恢复，因此有少量未使用函数告警；它们已被链接器垃圾回收，不进入有效调用路径。
